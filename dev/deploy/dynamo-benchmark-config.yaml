# Benchmark configuration targeting Dynamo inference server (minikube or other K8s cluster)
benchmark:
  endpoint:
    model_names: ["Qwen/Qwen3-0.6B"]
    urls: ["http://dynamo-agg-frontend.dynamo-server.svc.cluster.local:8000/v1"]
    type: chat

  tokenizer:
    name: Qwen/Qwen3-0.6B

loadgen:
  concurrency: 2
  request_count: 20
  warmup_request_count: 3

dataset:
  sequence_length:
    distribution: uniform
    min: 20
    max: 50
  output_tokens:
    distribution: uniform
    min: 10
    max: 30
