# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aiperf-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aiperf-operator.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "aiperf-operator.operatorSelectorLabels" . | nindent 4 }}
  ports:
  - name: health
    port: 8080
    targetPort: health
    protocol: TCP
  - name: results
    port: {{ .Values.resultsServer.port }}
    targetPort: results
    protocol: TCP
  {{- if gt (int .Values.operator.metrics.port) 0 }}
  - name: metrics
    port: {{ .Values.operator.metrics.port }}
    targetPort: metrics
    protocol: TCP
  {{- end }}
