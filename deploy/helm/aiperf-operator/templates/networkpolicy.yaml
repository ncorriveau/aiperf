# SPDX-FileCopyrightText: Copyright (c) 2025-2026 NVIDIA CORPORATION & AFFILIATES. All rights reserved.
# SPDX-License-Identifier: Apache-2.0
{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "aiperf-operator.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aiperf-operator.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "aiperf-operator.operatorSelectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    # Allow traffic from the release namespace (operator's own namespace — needed
    # for `helm test` hook pods to hit /healthz), the benchmark namespace, and
    # any configured additional namespaces. All hit health (8080) and results
    # (resultsServer.port).
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Release.Namespace | quote }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.benchmarkNamespace.name | quote }}
        {{- range .Values.benchmarkRbacNamespaces }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
        {{- end }}
        {{- range .Values.networkPolicy.allowedNamespaces }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
        {{- end }}
      ports:
        - protocol: TCP
          port: 8080
        - protocol: TCP
          port: {{ .Values.resultsServer.port }}
        {{- if gt (int .Values.operator.metrics.port) 0 }}
        - protocol: TCP
          port: {{ .Values.operator.metrics.port }}
        {{- end }}
    {{- with .Values.networkPolicy.allowedIngressCIDRs }}
    # Allow external scrapers (e.g. Prometheus, ingress controllers) on CIDR allow-list.
    - from:
        {{- range . }}
        - ipBlock:
            cidr: {{ . | quote }}
        {{- end }}
      ports:
        - protocol: TCP
          port: 8080
        - protocol: TCP
          port: {{ $.Values.resultsServer.port }}
        {{- if gt (int $.Values.operator.metrics.port) 0 }}
        - protocol: TCP
          port: {{ $.Values.operator.metrics.port }}
        {{- end }}
    {{- end }}
  egress:
    # DNS resolution (cluster DNS typically runs in kube-system).
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53
    # Kubernetes API server (443) — no standard selector, so allow all egress on 443/6443.
    - ports:
        - protocol: TCP
          port: 443
        - protocol: TCP
          port: 6443
    # Benchmark namespace (spawn/manage JobSets, talk to controller services).
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ .Values.benchmarkNamespace.name | quote }}
    {{- with .Values.benchmarkRbacNamespaces }}
    - to:
        {{- range . }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
        {{- end }}
    {{- end }}
    {{- with .Values.networkPolicy.allowedNamespaces }}
    - to:
        {{- range . }}
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: {{ . | quote }}
        {{- end }}
    {{- end }}
{{- end }}
